[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
asyncio_mode = auto
addopts = -v --tb=short

# Logging configuration (uncomment to enable debug logs)
# log_cli = true
# log_cli_level = DEBUG
# log_cli_format = %(asctime)s %(levelname)s %(name)s: %(message)s
# log_cli_date_format = %H:%M:%S
